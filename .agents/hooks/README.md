# Hooks

Hooks are deterministic checks.

Use hooks for things a script can verify, such as:

- blocking destructive commands
- checking formatting
- running lint or tests
- detecting forbidden files or patterns

Do not put judgment-heavy workflow logic in hooks.
