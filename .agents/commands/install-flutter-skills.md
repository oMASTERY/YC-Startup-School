# Install Flutter Agent Skills

Run this command after setting up a new Flutter project from this development kit to fetch the official, up-to-date AI agent skills from the Flutter team:

```bash
npx skills add flutter/skills --skill '*' --agent universal
```

## Update Skills

To update your installed skills to their latest versions from the repository, simply run:

```bash
npx skills update
```
